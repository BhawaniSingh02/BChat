I want you to specifically investigate and fix the current call window bugs in this app’s new calling UI. Right now, when a call window is created and I minimize, maximize, expand, or restore it, there are visual and layout bugs. In some cases the floating call window becomes stretched, clipped, misplaced, or partially off-screen, and in video calls when I click minimize both video surfaces disappear and the view becomes black/blank instead of continuing to show the local/remote video correctly. I want you to deeply inspect the full call UI state management, drag/minimize/maximize/restore logic, responsive sizing, z-index, bounds calculations, video container sizing, stream attachment lifecycle, and any WebRTC/video element remount behavior causing these issues. Fix the bugs so the floating call window behaves like a polished production feature: it should minimize cleanly into a compact movable mini-call widget, expand back to the correct full call panel, preserve aspect ratios, stay within viewport bounds, survive window resizing, and keep audio/video streams visible and attached correctly across all transitions. Do not just patch the symptoms; identify root causes such as component remounts, stale refs, incorrect CSS transforms, bad width/height persistence, drag math errors, video element detachment, or stream cleanup firing at the wrong time. The final behavior should feel smooth and premium like WhatsApp: stable mini-window, proper expand/collapse, draggable anywhere on screen, correct local preview and remote video rendering, no black screens on minimize/restore, no broken overlays, and no console errors. Please make the actual code changes, verify the relevant call flows, and add safeguards/tests where useful so this part of the calling system becomes reliable and professional.