spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/chatapp}

# Enforce ISO-8601 UTC timestamps from Jackson so the frontend can parse them reliably
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# LOCAL DEV ONLY — override with JWT_SECRET env var in production. NEVER use this default value in production.
jwt.secret=${JWT_SECRET:dGVzdC1zZWNyZXQta2V5LWZvci1kZXZlbG9wbWVudC1vbmx5LW5vdC1mb3ItcHJvZHVjdGlvbg==}
jwt.expiry-ms=${JWT_EXPIRY_MS:900000}

cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# Base URL used by local file storage to build file URLs (dev only — ignored when Cloudinary is configured)
app.base-url=${APP_BASE_URL:http://localhost:8080}

logging.level.com.substring.chat=DEBUG

spring.data.redis.url=${REDIS_URL:redis://localhost:6379}
app.presence.backend=${PRESENCE_BACKEND:memory}

# Cloudinary file uploads (optional — leave blank to disable uploads)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# Multipart upload limits
spring.servlet.multipart.max-file-size=51MB
spring.servlet.multipart.max-request-size=52MB

# Refresh token
refresh.expiry-days=30

# Email — Gmail SMTP (dev)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=baaat.app@gmail.com
spring.mail.password=ydvkdrxrkivzcsoa
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=baaat.app@gmail.com

# MongoDB auto-index creation
spring.data.mongodb.auto-index-creation=true

# Frontend base URL (used in email links)
app.frontend-url=${FRONTEND_URL:http://localhost:5173}
